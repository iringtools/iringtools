2012-09-08 17:59:37,165 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 17:59:37,626 [137] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:00:13,432 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:00:13,434 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:00:13,436 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:00:13,439 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:00:14,001 [137] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:09:33,918 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:09:34,004 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:09:34,020 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:09:34,071 [137] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:09:34,326 [137] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:43:18,425 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:43:18,553 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:43:18,568 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:43:18,582 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:43:19,065 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:43:53,008 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:43:53,010 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:43:53,011 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:43:53,014 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:43:53,529 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:44:36,260 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:44:36,281 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:44:36,295 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:44:36,313 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:44:36,779 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:46:55,501 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:46:55,521 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:46:55,538 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:46:55,554 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:46:56,117 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:47:21,369 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:47:21,881 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:47:21,883 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:47:21,885 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:47:21,887 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:52:17,089 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:52:17,109 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:52:17,123 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:52:17,139 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:52:17,613 [4] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:52:44,511 [4] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:54:48,668 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:54:49,302 [26] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 18:55:10,644 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:55:10,646 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:55:10,660 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:55:10,674 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:58:16,268 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 18:58:16,344 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 18:58:16,358 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 18:58:16,373 [26] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 18:58:16,931 [26] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 19:44:47,784 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 19:44:47,843 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 19:44:47,858 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 19:44:47,877 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 19:44:48,189 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 19:44:53,221 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 19:55:40,912 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 19:55:41,064 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 19:55:41,086 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 19:55:41,107 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 19:55:41,330 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:03:44,866 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:03:44,940 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:03:44,960 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:03:44,978 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:03:45,217 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:31:31,169 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:31:31,372 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:31:31,413 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:31:31,436 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:31:31,525 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:35:20,643 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:35:20,665 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:35:20,680 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:35:20,695 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:35:21,152 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:46:44,446 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:46:44,488 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:46:44,507 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:46:44,522 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:46:44,868 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:49:17,805 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:49:17,830 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:49:17,849 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:49:17,867 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:49:18,308 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 20:51:20,622 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 20:51:20,646 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 20:51:20,662 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 20:51:20,678 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 20:51:21,123 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 21:10:27,975 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 21:10:28,021 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 21:10:28,035 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 21:10:28,057 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 21:10:28,332 [85] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 21:12:22,261 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 21:12:22,283 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 21:12:22,299 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 21:12:22,318 [85] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 21:12:22,762 [91] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 21:19:07,697 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 21:19:07,777 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 21:19:07,793 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 21:19:07,813 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 21:19:08,085 [91] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 21:21:08,441 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 21:21:08,463 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 21:21:08,479 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 21:21:08,497 [91] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 21:21:08,956 [91] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 21:47:25,679 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 21:47:25,758 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 21:47:25,774 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 21:47:25,794 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 21:47:26,309 [64] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 22:02:02,958 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'EQUIPMENT'
2012-09-08 22:02:03,014 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'INSTRUMENTS'
2012-09-08 22:02:03,029 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'LINES'
2012-09-08 22:02:03,069 [64] DEBUG NHibernate.SQL - 
          select t2.name as column_name, type_name(t2.user_type_id) as data_type, 
          t2.max_length as data_length, t2.is_identity as is_identity, 
          t2.is_nullable as is_nullable, t4.column_id as is_primary_key from sys.objects t1
          inner join sys.columns t2 on t2.object_id = t1.object_id  
          left join sys.index_columns t4 on t4.object_id = t1.object_id and t4.column_id = t2.column_id
          left join sys.indexes t3 on t3.object_id = t1.object_id and t3.is_unique = 1
          and t3.index_id = t4.index_id
          where (upper(t1.type) = 'U' or upper(t1.type) = 'V') 
          and upper(schema_name(t1.schema_id)) = 'DBO' and upper(t1.name) = 'VALVES'
2012-09-08 22:02:03,281 [64] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
2012-09-08 22:02:38,262 [64] DEBUG NHibernate.SQL - 
          select name from sys.objects where (upper(type) = 'U' or upper(type) = 'V') 
          and upper(schema_name(schema_id)) = 'DBO' order by name
